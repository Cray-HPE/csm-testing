command:
  k8s_postgres_pods_per_cluster:
    title: Kubernetes Postgres Check that there are the Correct Number of Pods 'Running' Per Cluster
    meta:
      desc: If this test fails, refer to 'Troubleshoot Postgres Database' for more information on diagnosing and fixing the number of running pods per cluster.
      sev: 0
    exec: "{{.Env.GOSS_BASE}}/scripts/postgres_pods_running.sh"
    exit-status: 0
    stdout:
      - PASS
    timeout: 10000
    skip: false
