command:
  sdc_drive_master:
    title: Master Node sdc Drive
    meta:
      desc: Validates sdc drive is set up correctly on master nodes.
      sev: 0
    exec: lsblk | grep -A 2 'sdc' | grep 'etcdvg0'
    stdout:
    - /run/lib-etcd
    exit-status: 0