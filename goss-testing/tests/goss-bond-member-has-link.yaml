command:
  {{range $member := index .Vars "bond_members"}}
  bond_member_{{$member}}:
    stdout:
    - link
    exec: 'ip link show dev {{$member}}'
    exit-status: 0
    timeout: 1000
    skip: false
  {{end}}
