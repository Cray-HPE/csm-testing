command:
{{range $interface := index .Vars "interfaces"}}
  check_interface_{{$interface}}:
    exec: "ip addr show dev {{$interface}} scope global | grep 'inet ' | awk '{print $2}'"
    exit-status: 0
    timeout: 10000
    skip: false
{{end}}
